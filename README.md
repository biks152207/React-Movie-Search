# Instruction
```
npm install
gulp
cd dist
open index.html in the browser or if you have http-server simply http-server inside dist folder and it opens in certain port.
npm install http-server -g
```
